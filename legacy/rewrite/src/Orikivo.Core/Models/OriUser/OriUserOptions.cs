using System.Collections.Generic;

namespace Orikivo
{
    /*
    public class AccountOptions
    {
        public bool PlaylistAutoReplace;
        // if the playlist is full, automatically remove the oldest song.
        public bool AutoAdjustPayments;
        // if the money written is higher than the balance, set the value to the balance.
        public bool AutoPayFines;
        // if there are any fines appended to an account, automatically pay them off when there is enough to do so.
        public bool Tooltips;
        // quick snippets of examples and helper texts.
        public bool ThrowPrequelError {get; set;}
        // have errors be shown on commands with an error before it's even executed.
        public bool WordGuard {get; set;}
        // toggle profanity, link spam, etc.
        public ColorValueFormat ColorFormat {get; set;}
        // the format of the type of color that it will be shown as.
        public OutputFormat OutputFormat {get; set;}
        // how the results of what Orikivo sends are stylized as.
        public Locale Locale {get; set;}
        // language of orikivo
        public EmojiValueFormat EmojiFormat {get; set;}
        // the style of emojis when shown on orikivo.
        public string Prefix {get; set;}
        public string ClosingPrefix {get; set;}
        public List<string> BlockedWords {get; set;}
        public List<string> BlockedSites {get; set;}
        
    }
    */

    public enum ColorValueFormat { }
    public enum EmojiValueFormat { }
}