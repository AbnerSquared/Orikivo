namespace Orikivo
{
    public enum OccurrenceRate
    {
        Yearly = 1, // happens once a year.
        Monthly = 2, // happens once a month.
        Biweekly = 4, // happens every other week.
        Weekly = 8, // happens once a week.
        Bidaily = 16, // happens every other day.
        Daily = 32, // happens once a day.
        Once = 64 // only happens once.
    }
}
