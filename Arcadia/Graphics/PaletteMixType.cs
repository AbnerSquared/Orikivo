namespace Arcadia.Graphics
{
    public enum PaletteMixType
    {
        // mixes the colors as one flat new color
        Merge = 1,
        // transitions the colors from a to b
        Smear = 2
    }
}
