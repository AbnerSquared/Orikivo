namespace Orikivo
{
    // used to store and display nodes accordingly.
    public class NodeContainer
    {
        // NodeContainer => DisplayChannel (?)
        // instead of using multiple Display classes, make another class that can be passed into Display to easily swap out channels
    }
}
