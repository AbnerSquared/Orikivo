namespace Orikivo
{
    public class NodeConfig
    {
        // used to format the title
        public string TitleMap { get; set; }

        // used to format the entire content as a whole
        public string ContentMap { get; set; }

        // how the entire object is formatted.
        public string PropertyMap { get; set; }
    }
}
