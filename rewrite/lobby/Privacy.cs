namespace Orikivo
{
    public enum Privacy
    {
        Public = 1, // this is open to anyone with access to orikivo.
        Local = 2, // only the guild can view/use.
        Private = 3 // this is visible only for you.
    }
}
